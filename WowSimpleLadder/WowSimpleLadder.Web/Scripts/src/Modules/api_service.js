; (function () {
    var baseUrl = '/api/WowPvpLadder';

    window.apiService = {
        getPvpLadder = function() {
            var url = baseUrl + '/getpvpladder';
            var params = {
                
            };
        }
    };

})();